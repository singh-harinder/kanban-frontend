query User {
  authenticatedItem {
    ... on User {
      id
      name
      email
      board {
        id
        name
        section {
          id
          name
          tasks {
            id
          }
        }
      }
    }
  }
}
